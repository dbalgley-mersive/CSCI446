<%= form_for(@article_datum) do |f| %>
  <% if @article_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article_datum.errors.count, "error") %> prohibited this article_datum from being saved:</h2>

      <ul>
      <% @article_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label : edits %><br />
    <%= f.number_field : edits %>
  </div>
  <div class="field">
    <%= f.label :subtitle %><br />
    <%= f.text_field :subtitle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
