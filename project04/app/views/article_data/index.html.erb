<h1>Listing article_data</h1>

<table>
  <tr>
    <th> edits</th>
    <th>Subtitle</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @article_data.each do |article_datum| %>
  <tr>
    <td><%= article_datum. edits %></td>
    <td><%= article_datum.subtitle %></td>
    <td><%= link_to 'Show', article_datum %></td>
    <td><%= link_to 'Edit', edit_article_datum_path(article_datum) %></td>
    <td><%= link_to 'Destroy', article_datum, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Article datum', new_article_datum_path %>
